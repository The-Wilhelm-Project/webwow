'use client';

/**
 * Error boundary of the Webwow route group: reuses the published-site error
 * page (custom 500 page when one is configured, plain fallback otherwise).
 */
export { default } from '@/app/(site)/error';
